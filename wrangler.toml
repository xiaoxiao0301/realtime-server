name = "realtime-server"
main = "worker.js"
compatibility_date = "2023-12-01"

[vars]
ENVIRONMENT = "production"

# No Durable Objects needed for simplified deployment.
# (State is ephemeral per isolate.)

[build]
command = ""